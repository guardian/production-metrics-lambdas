regions: [eu-west-1]
stacks: [flexible]
deployments:
  production-metrics-lambdas:
    tyoe: aws-lambda
    parameters:
      bucket: composer-dist
      prefixStack: false
      fileName: production-metrics-lambdas.jar
      functionNames:
        - "flexible-production-metrics-lambdas-query-capi-"
